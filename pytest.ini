[pytest]
pythonpath = .
# Prevent memory exhaustion: limit workers and add garbage collection
addopts = -v --tb=short --maxfail=1
testpaths = tests
markers =
    integration: integration tests
    unit: unit tests